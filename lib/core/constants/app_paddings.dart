import 'package:flutter/material.dart';

class AppPaddings{
  static const EdgeInsets all = EdgeInsets.all(12);
  static const EdgeInsets horizontal = EdgeInsets.symmetric(horizontal: 24);
  static const EdgeInsets vertical = EdgeInsets.symmetric(vertical: 16);
  static const EdgeInsets figureCardAll = EdgeInsets.only(left: 25, top: 12, bottom: 12, right: 25);
  static const EdgeInsets ayetPadding = EdgeInsets.only(left: 30, right: 30, bottom:35);
}